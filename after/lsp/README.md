So apparently, putting a custom config in lsp/<server>.lua would be overloaded with
nvim-lspconfig defaults.

However, putting a custom config in after/lsp/<server>.lua, would merge it with defaults.
=> Better.
